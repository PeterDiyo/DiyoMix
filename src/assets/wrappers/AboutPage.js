import styled from "styled-components";

const Wrapper = styled.div`
  h3 {
    color: #2c3e50;
  }

  p {
    line-height: 2;
    color: var(--grey-700);
    margin-top: 2rem;
  }
`;

export default Wrapper;
